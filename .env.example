# Optional AI analysis key (DeepSeek / OpenAI-compatible token services).
# Prefer configuring provider + key in Settings → 模型 API 接口配置.
# This env var is only a fallback when config.json has no api_key.
STOCKAGENT_AI_API_KEY=
